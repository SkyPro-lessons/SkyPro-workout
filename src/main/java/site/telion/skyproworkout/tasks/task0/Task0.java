package site.telion.skyproworkout.tasks.task0;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Task0 {
    Logger logger = LoggerFactory.getLogger(Task0.class);
    String name;
    Integer age;
    Double salary;



















    /*

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        Task0 task0 = (Task0) o;

        if (name != null ? !name.equals(task0.name) : task0.name != null) return false;
        if (age != null ? !age.equals(task0.age) : task0.age != null) return false;
        return salary != null ? salary.equals(task0.salary) : task0.salary == null;
    }

    @Override
    public int hashCode() {
        int result = name != null ? name.hashCode() : 0;
        result = 31 * result + (age != null ? age.hashCode() : 0);
        result = 31 * result + (salary != null ? salary.hashCode() : 0);
        return result;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        Task0 task0 = (Task0) o;

        if (name != null ? !name.equals(task0.name) : task0.name != null) return false;
        if (age != null ? !age.equals(task0.age) : task0.age != null) return false;
        return salary != null ? salary.equals(task0.salary) : task0.salary == null;
    }

    @Override
    public int hashCode() {
        int result = name != null ? name.hashCode() : 0;
        result = 31 * result + (age != null ? age.hashCode() : 0);
        result = 31 * result + (salary != null ? salary.hashCode() : 0);
        return result;
    }

     */

}
